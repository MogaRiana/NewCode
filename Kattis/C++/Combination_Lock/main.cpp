#include <iostream>

using namespace std;

int main()
{
    int a, b, c, d, s = 720;
    while(cin >> a >> b >> c >> d){
        if(a == 0 && b == 0 && c == 0 && d == 0){
            break;
        }

    }
    return 0;
}
