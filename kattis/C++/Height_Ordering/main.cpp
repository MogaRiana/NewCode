#include <bits/stdc++.h>

using namespace std;
typedef long long i64;
typedef vector<i64> vi64;
typedef vector<vi64> vv;

int main() {
  i64 n;
  cin >> n;

  while (n--) {
    i64 a;
    cin >> a;
    vi64 v(20);
    for (i64 i = 0; i < 20; i++) {
      cin >> v[i];
    }
  }
  return 0;
}
