#include <bits/stdc++.h>

using namespace std;
typedef long long i64;
typedef int i32;
typedef short i16;
typedef vector<i64> vi64;
typedef vector<vi64> vv;

int main() {
  i32 n, m, q;
  cin >> n >> m;
  vector<i32> h(n);
  for (i32 i = 0; i < n; i++) {
    cin >> h[i];
  }
  cin >> q;

  vector<vector<i32>> v(m, vector<i32>(n));

  return 0;
}